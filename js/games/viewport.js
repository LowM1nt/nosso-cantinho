// Os joguinhos pixel rodam em qualquer tela (têm controles de toque: d-pad,
// toque nas colunas/objetos, arrastar, botões). Mantido como função pra ficar
// fácil desligar no futuro (ex.: voltar a "só desktop" -> usar matchMedia).
export const gamesEnabled = () => true;
